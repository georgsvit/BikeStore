@model IEnumerable<BikeStore.Models.View.AdminModel>

@{
    ViewData["Title"] = "Моделі";
}

<h1>Моделі</h1>

<p>
    <a class="btn btn-info" asp-action="Create">Створити модель</a>
</p>

<div class="text-light">
    <div class="row">
        @foreach (AdminModel adminModel in Model)
        {
            <partial name="AdminModelCard" model="adminModel" />
        }
    </div>
</div>